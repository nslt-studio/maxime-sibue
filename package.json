{
  "name": "maxime-sibue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "watch": "vite build --watch",
    "serve": "node dev-server.js",
    "tunnel": "npx cloudflared tunnel --url http://localhost:8787",
    "dev:live": "npm run watch & npm run serve & npm run tunnel"
  },
  "dependencies": {
    "@swup/preload-plugin": "^3.2.11",
    "swup": "^4.8.2"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
